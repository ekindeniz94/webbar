export interface ICloudflareCustomHostnameDetailsSslCertificate {
  issuer: string;
  serial_number: string;
  signature: string;
  expires_on: string;
  issued_on: string;
  fingerprint_sha256: string;
  id: string;
}
